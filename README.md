# Tobacco Flavor Classification via GA-Optimized Random Forest and SHAP Interpretation
Code for RSC Advances submission: Genetic Algorithm (GA) optimized Random Forest (RF) for tobacco flavor classification, combined with SHAP for feature interpretability.

## 1. Environment Setup
Install dependencies with pip:
```bash
pip install -r requirements.txt
Recommended Python version: 3.9 (compatible with all packages)
pandas==1.5.3
numpy==1.24.3
matplotlib==3.7.1
seaborn==0.12.2
scikit-learn==1.2.2
shap==0.41.0
deap==1.3.1
openpyxl==3.1.2
xlrd==2.0.1
xgboost==1.7.5
python>=3.9
